name: scoring:_environment
channels:
  - defaults
  - conda-forge   # recommended for newer Python/wheel coverage
dependencies:
  - python=3.9
  - pip
  - numpy
  - pandas
  - scikit-learn
  - joblib
  - matplotlib
  - seaborn  
  - pillow  
  - joblib
  - pip:
    - azureml-defaults
    - inference-schema[numpy-support]
